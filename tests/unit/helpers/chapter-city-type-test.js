import { chapterCityType } from 'september/helpers/chapter-city-type';
import { module, test } from 'qunit';

module('Unit | Helper | chapter city type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = chapterCityType([42]);
  assert.ok(result);
});
