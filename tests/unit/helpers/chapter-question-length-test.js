import { chapterQuestionLength } from 'september/helpers/chapter-question-length';
import { module, test } from 'qunit';

module('Unit | Helper | chapter question length');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = chapterQuestionLength([42]);
  assert.ok(result);
});
